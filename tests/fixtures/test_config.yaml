app:
  name: Test Lumen
  secret_key: test-secret-key-for-tests-only
  encryption_key: test-enc-key-for-tests-only
  dev_user: testuser@example.com
  database_url: sqlite:///test_lumen.db

admins:
  - admin@example.com

models:
  - model_name: test-model
    active: true
    input_cost_per_million: 1.0
    output_cost_per_million: 2.0
