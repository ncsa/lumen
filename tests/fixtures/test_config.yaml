version: 3

app:
  name: Test Lumen
  secret_key: test-secret-key-for-tests-only-0001
  encryption_key: test-encryption-key-for-tests-only-0001
  dev_user: testuser@example.com
  debug: true
  database:
    url: sqlite:///test_lumen.db

rate_limiting:
  storage_url: "memory://"

admins:
  - admin@example.com

models:
  - model_name: test-model
    active: true
    input_cost_per_million: 1.0
    output_cost_per_million: 2.0
