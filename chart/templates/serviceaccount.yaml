{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (include "lumen.fullname" .) }}
  labels:
    {{- include "lumen.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
